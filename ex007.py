#Desenvolta um programa que leia as duas notas de um aluno, calcule e mostra a sua média.
n1 = float(input('Digite a primeira nota: '))
n2 = float(input('Digite a segunda nota: '))

print('========CALCULANDO MÉDIA========')
print(f'A média entre as notas foi {(n1 + n2)/2:.1f}!')
